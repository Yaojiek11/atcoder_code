#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <math.h>
#include <set>
#include <map>
#include <stack>
#include <queue>
using namespace std;
using ll = long long;




int main(){
    int N, M;
    vector<int> p(N);
    for(int i=0; i<N; i++) cin >> p[i];

    vector<int> x(M), y(M);
    for(int i=0; i<M; i++){
        cin >> x[i] >> y[i];
    }

    int count=0;

    for(int i=0; i)
    return 0;
}